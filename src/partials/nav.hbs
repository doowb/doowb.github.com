    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <ul class="nav">
            {{#pages~}}{{#each items as |item|}}
            <li><a href="{{lowercase item.filename}}">{{item.basename}}</a></li>
            {{/each}}{{~/pages}}
          </ul>
          <ul class="nav"><li><a href="{{ assemble.repo }}">Visit Assemble</a></li></ul>
        </div>
      </div>
    </div>
