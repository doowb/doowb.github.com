<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    {{#each stylesheets}}
    <link rel="stylesheet" href="{{relative @root.dest @root.assets}}/css/{{.}}">
    {{/each}}
  </head>
  <body>
    {{> nav }}

    <div class="container">
      {% body %}
    </div>
    {{> ga }}
    {{#each scripts}}<script src="{{@root.assets}}/js/{{.}}"></script>{{/each}}
    {{> newsletter-signup }}
    {{> drift }}
  </body>
</html>

